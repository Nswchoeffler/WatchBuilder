export { RULES } from './registry';
export { TOL } from './tolerances';
export { candidatesFor, evaluate, requiredSlots, resolveParts } from './evaluate';
export type { BuildInput, BuildReport, BuildStatus, Candidate, Compatibility, UnresolvedRef } from './evaluate';
export type { Finding, PartLookup, PartSource, ResolvedParts, Rule, RuleResult, Severity } from './types';
